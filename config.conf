TIMEOUT=10

# Parallel jobs (keep <= 5 for ip-api free tier)
MAX_JOBS=4

# Delay between starting jobs (seconds, can be decimal)
JOB_DELAY=0.3

PING0_HOST="ping0-api.p.rapidapi.com"
PING0_KEY="PUT_YOUR_API_KEY_HERE"
